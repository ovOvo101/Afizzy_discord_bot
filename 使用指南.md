# Discord 创作 Bot 使用指南

这份指南适合第一次使用电脑命令、Discord 开发者门户或 Codex 的人。当前默认只启用 `invite-code` 频道的一人一次发言限制；每日投票、每日创作题和 `/idea` 已静默。

> 运行条件：一台保持开机、联网且不休眠的 Mac。电脑休眠、关机或断网时，Bot 会暂时离线；恢复后可以用 Codex 再次启动它。

## 第一步：准备项目

1. 从 GitHub 下载或克隆本项目。
2. 在 Mac 上打开 **终端**（按 `Command + 空格`，搜索“终端”）。
3. 进入项目文件夹。若项目在“下载”目录，示例命令是：

   ```bash
   cd ~/Downloads/Discord_bot
   ```

   不确定项目在哪时，可以在 Finder 中找到项目文件夹，右键点击后选择“服务” → “新建位于文件夹位置的终端窗口”。

4. 安装 Setup Skill：

   ```bash
   mkdir -p ~/.codex/skills
   cp -R skills/discord-bot-setup ~/.codex/skills/discord-bot-setup
   ```

5. 重新打开 Codex，或新建一个对话，然后说：“使用 `discord-bot-setup` 帮我初始化这个 Discord Bot 项目。”它会创建 Python 环境、安装依赖、建立本地配置，并检查题库；不会读取或显示你的 Token。

## 第二步：创建 Discord Bot

1. 打开 [Discord Developer Portal](https://discord.com/developers/applications)，点击 **New Application**，填写名字后创建。
2. 点击左侧 **Bot**。可选地修改 Bot 名称和头像。
3. 在 **Token** 区域点击 **Reset Token**，复制完整 Token。

   > Token 相当于 Bot 的密码。不要把它发给任何人、放到截图中，或提交到 GitHub。

4. 在同一页的 **Privileged Gateway Intents** 中，打开 **Message Content Intent**。
5. 打开左侧 **Installation**：
   - 选择服务器安装（Guild Install）。
   - Scopes 勾选 `bot` 和 `applications.commands`。
   - Bot 权限勾选：查看频道、发送消息、管理消息、嵌入链接、阅读消息历史记录、使用应用程序命令、创建投票。
   - 不要勾选“管理员”。
6. 复制安装链接，在浏览器地址栏打开它；选择你的 Discord 服务器并授权。

如果 Bot 是私密 App，同时看到“私密 APP 无法拥有默认授权关联”的提示：到 Installation 页面把默认安装链接设为 `None（无）`，然后用 OAuth2 URL Generator 生成邀请链接安装到自己的服务器。

## 第三步：保存 Token 和服务器设置

用纯文本编辑器打开 `.env`。最简单的方式是：

```bash
nano .env
```

把这一行中的内容替换为你的完整 Token：

```env
DISCORD_TOKEN=粘贴你的BotToken到这里
```

保存 nano：按 `Control + O`，按回车，再按 `Control + X`。

保存 Token 后回到 Codex 说“Token 已保存，继续设置”。它会请你提供服务器和频道 ID，并协助完成下列设置。接着在 Discord 客户端开启“开发者模式”：**用户设置** → **高级** → 打开“开发者模式”。然后：

- 右键服务器名称 → “复制服务器 ID”。
- 右键希望发投票的文字频道 → “复制频道 ID”。
- 右键希望发创作题的文字频道 → “复制频道 ID”。

首次测试时，可以让 Codex 将 `poll_time` 和 `prompt_time` 改成当前时间之后的几分钟。时间采用 `Asia/Shanghai` 时区，格式必须是 `24小时制 HH:MM`，例如 `18:30`。

## 第四步：让 Codex 管理 Bot

Setup Skill 会安装管理 Skill。之后直接用自然语言说：

- “启动 Bot”
- “检查 Bot 状态”
- “看最近 50 行 Bot 日志”
- “停止 Bot”

Codex 会安全地启动或停止本地 Bot，并在 `runtime/` 保存日志、运行状态、SQLite 数据库和题库备份。

## 第五步：测试功能

1. 用同一账号在名为 `invite-code` 的频道连续发两条消息。
2. 第一条应保留，第二条应被 Bot 删除，并收到英文私信提醒。
3. 确认 `/idea` 不再出现，且 Bot 不会发布每日投票或每日创作题。

发言次数在 Bot 重启后仍会保留。灵感功能已与限流功能隔离；只有将 `features.inspiration` 明确设为 `true` 才会重新启用。

## 用自然语言管理内容

安装 Skill 后，不需要手动编辑 YAML。可以对 Codex 说：

- “查看当前投票时间”
- “把投票时间改为 18:30”
- “列出所有 OC 创作题”
- “添加一条 OC 创作题：给你的 OC 设计一件不适合 TA 的礼服”
- “查看投票 `modern-job`”
- “删除创作题 `oc-01`”

删除时 Codex 会先显示将删除的内容，只有你明确确认后才会执行。每次改动都会先校验并保留备份；如果 Bot 正在运行，改动会自动重启 Bot 以生效。

## 常见问题

### Bot 显示离线

对 Codex 说“检查 Bot 状态”。若未运行，说“启动 Bot”。如果电脑刚从睡眠恢复，也建议执行一次检查。

### Bot 无法发消息或创建投票

检查 Bot 在目标频道是否拥有“查看频道、发送消息、嵌入链接、阅读消息历史记录、创建投票”的权限。不要授予管理员权限。

### Token 无效或泄露

回到 Discord Developer Portal 的 Bot 页面，点击 Reset Token，更新 `.env` 中的 `DISCORD_TOKEN`，然后对 Codex 说“重启 Bot”。旧 Token 会立即失效。

### 想把项目分享给别人

分享 GitHub 仓库即可。不要分享你的 `.env` 或 `config/config.yaml`；对方应从 `.env.example` 和 `config/config.example.yaml` 建立自己的本地文件。
