# Discord Bot 分享与安装指南

这份说明发给已获授权的协作者。默认你已经通过**私密方式**向对方发送了：

- Bot Token；
- `discord-bot-setup.skill` 文件；
- 项目地址：<https://github.com/ovOvo101/Discord_bot>。

> Token 就是 Bot 的密码。不要发到群聊、GitHub、截图或 Codex 对话中；只应写入对方自己的电脑。

## 1. 下载项目

直接对 Codex 说：

> 请从 https://github.com/ovOvo101/Discord_bot 克隆项目，并在本地打开它。

## 2. 安装 Setup Skill

1. 在 Codex 中打开收到的 `discord-bot-setup.skill` 文件。
2. 按 Codex 的安装提示完成导入。
3. 回到刚刚打开的 `Discord_bot` 项目，新建一个 Codex 对话。
4. 发送：

   > 使用 `discord-bot-setup` 帮我初始化这个 Discord Bot 项目。

Codex 会自动创建运行环境、安装依赖、生成本机配置文件，并检查题库。过程中不需要手动编辑 YAML。

## 3. 在本机填写 Token

当 Codex 提示 Token 尚未设置时：

1. 在项目文件夹打开终端。
2. 输入：

   ```bash
   nano .env
   ```

3. 找到这一行：

   ```env
   DISCORD_TOKEN=replace-me
   ```

4. 将 `replace-me` 替换为私下收到的完整 Token。
5. 按 `Control + O` 保存，按回车确认，再按 `Control + X` 退出。
6. 回到 Codex，只需说：

   > Token 已保存，继续设置。

不要把 Token 发给 Codex。Setup Skill 只检查它是否已经填写，不会读取或显示具体内容。

## 4. 填写 Discord 服务器和频道

Codex 会询问一个非秘密 ID：服务器 ID。

1. 在 Discord 打开 **用户设置** → **高级** → 开启“开发者模式”。
2. 右键服务器名称，选择“复制服务器 ID”。
3. 将服务器 ID 提供给 Codex；它会安全写入本机配置。

若 Bot 还没被邀请进服务器，请在 Discord Developer Portal 为它生成安装链接。需要勾选 `bot` 和 `applications.commands`，并授予：查看频道、发送消息、管理消息和阅读消息历史记录。不要授予“管理员”。

## 5. 启动与测试

对 Codex 说：

> 启动 Bot。

然后依次说：

- “检查 Bot 状态”；
- “看最近 50 行 Bot 日志”。

最后用同一个普通成员账号在 `invite-code` 频道连续发送两条消息：第一条应保留，第二条应被删除并收到英文私信。默认不应出现 `/idea`，Bot 也不会发布每日 Poll 或 Prompt。

## 6. 日常管理

后续都可以直接对 Codex 说：

- “检查 Bot 状态”；
- “查看当前功能配置”；
- “停止 Bot”。

配置、题库、日志和数据库均只保存在对方自己的电脑；不需要提交或推送到 GitHub。

## 注意事项

- 同一个 Token 同时只运行一份正式 Bot，否则可能重复处理成员消息。
- Mac 休眠、关机或断网时，Bot 会离线。
- 不再信任某位协作者时，立即到 Discord Developer Portal 的 **Bot** 页面点击 **Reset Token**；旧 Token 会失效。
